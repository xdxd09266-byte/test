import { yellow } from "@jsr/std__fmt/colors";
import { getRuntimeName } from "../../runtime/runtime_name.js";
export function createTestFunction(runTest) {
  return (nameOrOptionsOrFn, fn)=>{
    if (typeof nameOrOptionsOrFn === "string") {
      return runTest({
        name: nameOrOptionsOrFn,
        fn: fn
      });
    } else if (typeof nameOrOptionsOrFn === "function") {
      return runTest({
        name: nameOrOptionsOrFn.name || "unnamed test",
        fn: nameOrOptionsOrFn
      });
    }
    if (Array.isArray(nameOrOptionsOrFn.ignore) ? nameOrOptionsOrFn.ignore.includes(getRuntimeName()) : nameOrOptionsOrFn.ignore) {
      console.warn(yellow("skip: %s"), nameOrOptionsOrFn);
    }
    return runTest(nameOrOptionsOrFn);
  };
}
//# sourceMappingURL=create_test_function.js.map