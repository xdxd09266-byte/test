export async function createTest() {
  if ("Deno" in globalThis) {
    const { createDenoTestFunction } = await import("./runtime/deno.js");
    return createDenoTestFunction();
  } else if ("Bun" in globalThis) {
    const { createBunTestFunction } = await import("./runtime/bun.js");
    return createBunTestFunction();
  } else if ("process" in globalThis) {
    const { createNodeTestFunction } = await import("./runtime/node.js");
    return createNodeTestFunction();
  } else {
    throw new Error("unsupported runtime");
  }
}
//# sourceMappingURL=create_test.js.map