import { type Border } from "./border.js";
import { type Direction } from "./cell.js";
import { Column, type ColumnOptions } from "./column.js";
import { type DataRow, Row, type RowType } from "./row.js";
/** Border characters settings. */ export type BorderOptions = Partial<Border>;
/** Table settings. */ export interface TableSettings {
  /** Table indentation. */ indent: number;
  /** Enable/disable border on all cells. */ border: boolean;
  /** Set min column width. */ minColWidth: number | Array<number>;
  /** Set max column width. */ maxColWidth: number | Array<number>;
  /** Set max table width. */ maxWidth: number;
  /**
   * Set column flex-shrink weight. Follows CSS flex-shrink semantics: reduction
   * is proportional to `weight × width`. 0 = no shrink (default), >= 1 = shrinkable.
   */ flexShrink: number | Array<number>;
  /**
   * Set column flex-grow weight. Follows CSS flex-grow semantics: available
   * slack is distributed proportionally by weight. 0 = no grow (default), >= 1 = growable.
   */ flexGrow: number | Array<number>;
  /** Set cell padding. */ padding: number | Array<number>;
  /** Set table characters. */ chars: Border;
  /** Set cell content alignment. */ align?: Direction;
  /** Set column options. */ columns: Array<Column>;
}
/** Table type. */ export type TableType<TRow extends RowType = RowType> = Array<TRow> | Table<TRow>;
/**
 * Table representation.
 *
 * ```ts
 * import { Row, Table } from "./mod.ts";
 *
 * new Table()
 *   .header(new Row("Name", "Date", "City", "Country").border())
 *   .body([
 *     ["Baxter Herman", "Oct 1, 2020", "Harderwijk", "Slovenia"],
 *     ["Jescie Wolfe", "Dec 4, 2020", "Alto Hospicio", "Japan"],
 *     ["Allegra Cleveland", "Apr 16, 2020", "Avernas-le-Bauduin", "Samoa"],
 *     ["Aretha Gamble", "Feb 22, 2021", "Honolulu", "Georgia"],
 *   ])
 *   .render();
 * ```
 */ export declare class Table<TRow extends RowType = RowType> extends Array<TRow> {
  protected static _chars: Border;
  protected options: TableSettings;
  private headerRow?: any;
  /**
   * Create a new table. If rows is a table, all rows and options of the table
   * will be copied to the new table.
   *
   * @param rows An array of rows or a table instance.
   */ public static override from<TRow extends RowType>(rows: TableType<TRow>): Table<TRow>;
  /**
   * Create a new table from an array of json objects. An object represents a
   * row and each property a column.
   *
   * @param rows Array of objects.
   */ public static fromJson(rows: Array<DataRow>): Table;
  /**
   * Set global default border characters.
   *
   * @param chars Border options.
   */ public static chars(chars: BorderOptions): typeof Table;
  /**
   * Write table or rows to stdout.
   *
   * @param rows Table or rows.
   */ public static render<TRow extends RowType>(rows: TableType<TRow>): void;
  /**
   * Read data from an array of json objects. An object represents a
   * row and each property a column.
   *
   * @param rows Array of objects.
   */ public fromJson(rows: Array<DataRow>): this;
  /**
   * Set column options.
   *
   * @param columns An array of columns or column options.
   */ public columns(columns: Array<Column | ColumnOptions>): this;
  /**
   * Set column options by index.
   *
   @param index   The column index.
   @param column  Column or column options.
   */ public column(index: number, column: Column | ColumnOptions): this;
  /**
   * Set table header.
   *
   * @param header Header row or cells.
   */ public header(header: RowType): this;
  /**
   * Set table body.
   *
   * @param rows Array of rows.
   */ public body(rows: Array<TRow>): this;
  /** Clone table recursively with header and options. */ public clone(): Table;
  /** Generate table string. */ public override toString(): string;
  /** Write table to stdout. */ public render(): this;
  /**
   * Set max column width.
   *
   * @param width     Max column width.
   * @param override  Override existing value.
   */ public maxColWidth(width: number | Array<number>, override?: boolean): this;
  /**
   * Set min column width.
   *
   * @param width     Min column width.
   * @param override  Override existing value.
   */ public minColWidth(width: number | Array<number>, override?: boolean): this;
  /**
   * Set max table width
   *
   * @param width     Max table width.
   * @param override  Override existing value.
   */ public maxWidth(width: number, override?: boolean): this;
  /**
   * Set column flex-shrink weight. Follows CSS flex-shrink semantics: each
   * column's share of the reduction is proportional to `weight × width`, so
   * a wider column or one with a higher weight absorbs more of the overflow.
   * 0 = no shrink (default), >= 1 = shrinkable.
   *
   * @param flexShrink  Per-column shrink weight, or a single value for all columns.
   * @param override    Override existing value.
   */ public flexShrink(flexShrink?: number | Array<number>, override?: boolean): this;
  /**
   * Set column flex-grow weight. Follows CSS flex-grow semantics: available
   * slack is distributed proportionally by weight, so weight 2 receives twice
   * the extra space of weight 1. 0 = no grow (default), >= 1 = growable.
   *
   * @param flexGrow  Per-column grow weight, or a single value for all columns.
   * @param override  Override existing value.
   */ public flexGrow(flexGrow?: number | Array<number>, override?: boolean): this;
  /**
   * Shorthand to set both flex-grow and flex-shrink to the same value.
   * Columns will both expand into and contract out of
   * available space proportionally. 0 = rigid (default), >= 1 = flexible.
   *
   * @param flex      Per-column weight, or a single value for all columns.
   * @param override  Override existing value.
   */ public flex(flex?: number | Array<number>, override?: boolean): this;
  /**
   * Set table indentation.
   *
   * @param width     Indent width.
   * @param override  Override existing value.
   */ public indent(width: number, override?: boolean): this;
  /**
   * Set cell padding.
   *
   * @param padding   Cell padding.
   * @param override  Override existing value.
   */ public padding(padding: number | Array<number>, override?: boolean): this;
  /**
   * Enable/disable cell border.
   *
   * @param enable    Enable/disable cell border.
   * @param override  Override existing value.
   */ public border(enable?: boolean, override?: boolean): this;
  /**
   * Align table content.
   *
   * @param direction Align direction.
   * @param override  Override existing value.
   */ public align(direction: Direction, override?: boolean): this;
  /**
   * Set border characters.
   *
   * @param chars Border options.
   */ public chars(chars: BorderOptions): this;
  /** Get table header. */ public getHeader(): Row | undefined;
  /** Get table body. */ public getBody(): Array<TRow>;
  /** Get max column width. */ public getMaxColWidth(): number | Array<number>;
  /** Get min column width. */ public getMinColWidth(): number | Array<number>;
  /** Get table indentation. */ public getIndent(): number;
  /** Get cell padding. */ public getPadding(): number | Array<number>;
  /** Check if table has border. */ public getBorder(): boolean;
  /** Check if header row has border. */ public hasHeaderBorder(): boolean;
  /** Check if table bordy has border. */ public hasBodyBorder(): boolean;
  /** Check if table header or body has border. */ public hasBorder(): boolean;
  /** Get table alignment. */ public getAlign(): Direction;
  /** Get columns. */ public getColumns(): Array<Column>;
  /** Get column by column index. */ public getColumn(index: number): Column;
}
//# sourceMappingURL=table.d.ts.map