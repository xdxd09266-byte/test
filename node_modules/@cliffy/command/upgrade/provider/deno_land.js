import { Provider } from "../provider.js";
export class DenoLandProvider extends Provider {
  name = "deno.land";
  repositoryUrl = "https://deno.land/x/";
  registryUrl = "https://deno.land/x/";
  apiUrl = "https://cdn.deno.land/";
  moduleName;
  constructor({ name, main, logger } = {}){
    super({
      main,
      logger
    });
    this.moduleName = name;
  }
  async hasRequiredPermissions() {
    const apiUrl = new URL(this.apiUrl);
    const permissionStatus = await Deno.permissions.query({
      name: "net",
      host: apiUrl.host
    });
    return permissionStatus.state === "granted";
  }
  async getVersions(name) {
    const response = await fetch(`${this.apiUrl}${this.moduleName ?? name}/meta/versions.json`);
    if (!response.ok) {
      throw new Error("couldn't fetch the latest version - try again after sometime");
    }
    return await response.json();
  }
  getRepositoryUrl(name, version) {
    return new URL(`${this.moduleName ?? name}${version ? `@${version}` : ""}`, this.repositoryUrl).href;
  }
  getRegistryUrl(name, version) {
    return new URL(`${this.moduleName ?? name}@${version}`, this.registryUrl).href;
  }
}
//# sourceMappingURL=deno_land.js.map