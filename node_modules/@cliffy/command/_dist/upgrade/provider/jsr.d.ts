import { Provider, type ProviderOptions, type Versions } from "../provider.js";
type Semver = `${number}.${number}.${number}` | `${number}.${number}.${number}-${string}`;
type Package = `@${string}/${string}`;
export type JsrProviderOptions = ProviderOptions & ({
  package: Package;
} | {
  scope: string;
  name?: string;
});
export declare class JsrProvider extends Provider {
  name: string;
  protected readonly delegateLatestResolution: true;
  private readonly repositoryUrl: any;
  private readonly packageName?: any;
  private readonly packageScope: any;
  constructor({}: JsrProviderOptions);
  hasRequiredPermissions(): Promise<boolean>;
  getVersions(name: string): Promise<Versions>;
  getRepositoryUrl(name: string, version?: Semver): string;
  getRegistryUrl(name: string, version: string): string;
}
//# sourceMappingURL=jsr.d.ts.map