import { Provider, type ProviderOptions, type Versions } from "../provider.js";
export interface GithubProviderOptions extends ProviderOptions {
  repository: string;
  branches?: boolean;
  token?: string;
}
export interface GithubVersions extends Versions {
  tags: Array<string>;
  branches: Array<string>;
}
export declare class GithubProvider extends Provider {
  name: string;
  private readonly repositoryUrl: any;
  private readonly registryUrl: any;
  private readonly apiUrl: any;
  private readonly repositoryName: any;
  private readonly listBranches?: any;
  private readonly githubToken?: any;
  constructor({}: GithubProviderOptions);
  hasRequiredPermissions(): Promise<boolean>;
  getVersions(_name: string): Promise<GithubVersions>;
  getRepositoryUrl(_name: string, version?: string): string;
  getRegistryUrl(_name: string, version: string): string;
  override listVersions(name: string, currentVersion?: string): Promise<void>;
  private getApiUrl: any;
  private gitFetch: any;
}
//# sourceMappingURL=github.d.ts.map